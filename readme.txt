Use java -cp bin/ "file name" to run the applications

Each date/time in the DateTimeList textfile need to be on a different line in order for the method to work. The textfile needs to be named DateTimeList
